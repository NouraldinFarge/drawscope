{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal local DrawScope window capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
